{{! The 'content' variable contains the markup generated from the relevant
markdown content file. All other variables, e.g. the 'title' used here, will
come from the metadata for that piece of content. }}

<h1>{{title}}</h1>
{{{content}}}
